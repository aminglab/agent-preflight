# OpenClaw adapter

This directory is reserved for the first public adapter layer.

The OpenClaw adapter will let OpenClaw delegate high-risk browser tasks to Agent Preflight for rehearsal before real submission.
