# Admin config change example

This example will demonstrate a browser workflow where a runtime wants to change an admin or configuration setting.

The purpose is to rehearse alternative navigation and submission paths before touching the real system.
